A chield function can have access all the properties of its parrent
A closure is a function having access to the parent scope, even after the parent function has closed.

//scope: a piace of code wich is bounded in curly braces is nothing but scope